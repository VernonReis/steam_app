<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <title></title>
  </head>
  <body>

  <div id="topBar">
    <div id="pageTitle">
      <h1>User Games</h1>
    </div>
    <div id="logout">
      <a href="/user/logout">Logout</a>
    </div>
  </div>

  <div id="container">


  <nav>
  <a href="/user/games/new" >Add New Game to Library</a>
  </nav>
  <div id="entryContainer">
    <% if (hasGames) { %>
      <% for (game of games) { %>
        <div class="entry">
          <p><%= game.title %></p>
          <div class="gameImage">
            <% if (game.img != "") { console.log("baggy"); %>
            <img src="<%= game.img %>" >
            <% } else { %>
            <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/a/ac/No_image_available.svg/450px-No_image_available.svg.png">
            <% } %>
            <% console.log(typeof game.title) %>
          </div>
        
          <a href="/user/games/<%= game._id %>">View Details</a>

        </div>
      <% } %>
    <% } %>
  </div>
  </div>  
  </body>
</html>