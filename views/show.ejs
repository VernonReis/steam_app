<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <title></title>
  </head>
  <body>

  <div id="topbar">

  </div>

  <div id="container">

  <% if (hasGames) { %>
    <% for (game of games) { %>
      <div class="entry">
      <p><%= game.title %></p>
      <img src="http://media.steampowered.com/steamcommunity/public/images/apps/<%= game.appid %>/<%= game.img_logo_url %>.jpg">
      <a href="/user/games/edit/<%= game._id %>">Edit</a>
      <form action="/user/games/delete?_method=DELETE" method="post">
        <input type="submit" value="Remove from library">
        <input type="hidden" name="id" value="<%= game._id %>">
      </form>

      </div>
    <% } %>
  <% } %>
  </div>  
  </body>
</html>